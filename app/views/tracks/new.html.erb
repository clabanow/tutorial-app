<div class='admin-form'>
  <h1 class='admin-tutorial'>Add a track</h1>

  <%= form_for(@track) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>

    <div class='form-field'>
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>

    <%= f.submit "Add track", class: 'btn submit-btn' %>
  <% end %>
</div>